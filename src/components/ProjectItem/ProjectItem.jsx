import { ListItem } from '@mui/material';

export const ProjectItem = ({ project }) => {
  // const { title, description, techSkills, imgUrl, demoUrl, sources, date } = project;

  return <ListItem disablePadding></ListItem>;
};
